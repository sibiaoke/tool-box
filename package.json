{
  "name": "tool-box",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && lerna bootstrap",
    "ver": "lerna version --amend --no-git-tag-version --no-push --yes",
    "lint": "eslint \"packages/**/src/**\"",
    "build": "lerna run build",
    "prebuild": "npm run bootstrap"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.11.1",
    "eslint-config-prettier": "^3.3.0",
    "eslint-plugin-prettier": "^3.0.1",
    "lerna": "^3.6.0",
    "prettier": "^1.15.3"
  },
  "license": "ISC"
}
